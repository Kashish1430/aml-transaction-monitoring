[tool.ruff]
line-length = 100
target-version = "py312"
exclude = ["notebooks", "venve", "investigations"]

[tool.ruff.lint]
select = ["E", "F", "I", "W"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
